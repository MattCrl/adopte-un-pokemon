{% extends 'base.html.twig' %}

{% block body %}
    <header class="header">
        <div class="container search-bar mt-5">
            <h1>Bienvenue sur adopeUnPokemon.com !</h1>
            <p class="ads-count">Actuellement {{ ads|length }} annonces en ligne</p>
        <h2 class="mb-4 main-title"><i class="fas fa-search"></i> RECHERCHER UN POKEMON</h2>

            {{ form_start(form, {'action': path('search_results'), 'method': 'POST'}) }}

        {{ form_end(form) }}
        </div>

    </header>
    <div class="container">



        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    </div>

        <section class="actual-ad pt-5 pb-5">
        <div class="container">
        <h2 class="mb-4 main-title">EN VENTE ACTUELLEMENT</h2>
            <h3 class="mt-2">Nos dernières annonces ! <br> Pour en voir plus, rendez-vous <a href="{{ path('ad_index') }}">ici</a>.</h3>

        <div class="row cards-bloc">
        {% for ad in ads %}
            <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                <div class="card cards-custom mt-4">
                    <img class="card-img-top" src="{{ ad.pokemon.image }}" alt="Card image cap">
                    <div class="card-body">
                        <h5 class="card-title text-center font-weight-bold">{{ ad.pokemon.name }}</h5>
                        <h6 class="text-center font-weight-bold">{{ ad.price }} €</h6>
                        <a href="{{ path('ad_show', {'id': ad.id}) }}" class="btn btn-primary btn-block">Voir l'annonce</a>
                    </div>
                </div>
            </div>
        {% endfor %}
        </div>
        </div>
        </section>

    <hr>




{% endblock %}
