{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('build/css/badges-type.css') }}">
{% endblock %}

{% block body %}
    <div class="banner">

    </div>

    <div class="container">
        <div class="mt-5 mb-5">
            <h1 class="text-uppercase">Mes pokémon vendus</h1>
        </div>

        {{ pagerfanta(my_pager, 'twitter_bootstrap4', { 'omitFirstPage': true}) }}

        <div class="row">
            {% for ad in my_pager %}
                {% include 'cards-template.html.twig' %}
            {% endfor %}
        </div>

        <h2 class="mt-3">Somme gagnée grâce à adopteUnPokemon.com :</h2>

        <span class="badge badge-primary badge-price">{{ sumPrice[0]['sumPrice'] }}€</span>


    </div>
{% endblock %}